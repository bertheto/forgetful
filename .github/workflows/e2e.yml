name: E2E Tests

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  e2e:
    # Fork guard: Postgres e2e tests only run on upstream (expensive + redundant on private fork).
    if: github.repository == 'ScottRBK/forgetful'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Cache FastEmbed models
        uses: actions/cache@v3
        with:
          path: ~/.cache/fastembed
          key: fastembed-${{ hashFiles('**/pyproject.toml') }}
          restore-keys: |
            fastembed-

      - name: Run E2E tests
        run: uv run pytest tests/e2e/ -v -m e2e
     